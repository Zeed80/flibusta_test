# Часто задаваемые вопросы (FAQ) - Flibusta Local Mirror

Этот документ содержит ответы на часто задаваемые вопросы о проекте Flibusta Local Mirror.

## Общие вопросы

### Что такое Flibusta Local Mirror?

Flibusta Local Mirror - это Docker-контейнеризованное веб-приложение для создания локальной копии библиотеки Флибусты. Проект предоставляет веб-интерфейс для поиска и просмотра книг, а также поддержку OPDS (Open Publication Distribution System) для интеграции с читалками электронных книг.

### Для кого предназначен этот проект?

Проект предназначен для:
- Частных лиц, желающих создать локальную копию библиотеки Флибусты
- Организаций, нуждающихся в оффлайн-доступе к библиотеке
- Исследователей и разработчиков, изучающих электронные библиотеки

### Какова лицензия проекта?

Проект распространяется под лицензией GNU General Public License v2.0. Подробности см. в файле [LICENSE](../LICENSE).

### Это официальный проект Флибусты?

Нет, это форк оригинального проекта [zlsl/flibusta](https://github.com/zlsl/flibusta) с существенными улучшениями и модернизацией.

## Установка и настройка

### Какая минимальная конфигурация сервера требуется?

**Минимальные требования:**
- **ОС**: Ubuntu 18.04+, Debian 10+, Rocky Linux 8+, CentOS Stream 8+
- **Docker**: версия 20.10 или выше
- **Docker Compose**: версия 2.0 или выше
- **Свободное место на диске**: минимум 10 GB (рекомендуется 20+ GB для полной библиотеки)
- **Оперативная память**: минимум 2 GB (рекомендуется 4+ GB)
- **Процессор**: минимум 2 ядра (рекомендуется 4+ ядер)

### Можно ли установить на Windows?

Технически возможно, но проект оптимизирован для Linux (Debian, Ubuntu Server). Для Windows требуется WSL2 (Windows Subsystem for Linux) или виртуальная машина.

### Можно ли установить на macOS?

Да, можно. Убедитесь, что установлены Docker Desktop for Mac и Docker Compose. Процедура установки аналогична Linux.

### Какие порты используются по умолчанию?

- **27100** - веб-сервер (Nginx)
- **27101** - база данных (PostgreSQL)

Порты можно изменить в файле `.env`:
```bash
FLIBUSTA_PORT=8080
FLIBUSTA_DB_PORT=8081
```

### Как изменить порт после установки?

1. Отредактируйте файл `.env`
2. Измените `FLIBUSTA_PORT` или `FLIBUSTA_DB_PORT`
3. Перезапустите контейнеры:
```bash
docker-compose restart
```

### Как изменить пароль базы данных?

1. Сгенерируйте новый пароль:
```bash
openssl rand -base64 24 | tr -d "=+/" | cut -c1-32
```

2. Обновите файл `.env`:
```bash
sed -i "s/FLIBUSTA_DBPASSWORD=.*/FLIBUSTA_DBPASSWORD=newpassword/" .env
```

3. Обновите файл секретов:
```bash
echo "newpassword" > secrets/flibusta_pwd.txt
chmod 600 secrets/flibusta_pwd.txt
```

4. Обновите пароль в PostgreSQL:
```bash
docker-compose exec -T postgres psql -U postgres -c "ALTER USER flibusta PASSWORD 'newpassword';"
```

5. Перезапустите контейнеры:
```bash
docker-compose restart
```

### Как использовать внешнюю базу данных?

Если у вас уже есть PostgreSQL сервер, вы можете использовать его вместо контейнера. Подробная документация см. в [application/tools/external_services_config/README.md](../application/tools/external_services_config/README.md).

### Можно ли использовать SQLite вместо PostgreSQL?

Нет, проект поддерживает только PostgreSQL. PostgreSQL выбран из-за производительности, надежности и поддержки полнотекстового поиска.

## Работа с данными

### Где находятся файлы данных?

По умолчанию файлы данных находятся в директориях проекта:
- `FlibustaSQL/` - SQL файлы дампа базы данных
- `Flibusta.Net/` - архивы книг (*.zip)
- `cache/` - кэш (авторы, обложки, временные файлы)

Пути можно изменить в `docker-compose.yml` или через переменные окружения.

### Как добавить новые книги?

Есть несколько способов:

**Вариант 1: Автоматическое обновление**
```bash
# Скачивание ежедневных обновлений
./update_daily.sh
```

**Вариант 2: Ручное добавление архивов**
1. Скопируйте архивы книг (*.zip) в директорию `Flibusta.Net/`
2. Откройте веб-интерфейс: `http://localhost:27100/service/`
3. Нажмите кнопку "Сканирование ZIP"

**Вариант 3: Полное обновление**
```bash
# Скачивание полного архива с Флибусты
wget http://flibusta.is/sql/f.n.fb2-000001-999999.zip -P Flibusta.Net/
```

### Как обновить базу данных?

**Вариант 1: Через веб-интерфейс**
1. Откройте http://localhost:27100/service/
2. Нажмите кнопку "Обновить базу"
3. Подождите завершения процесса

**Вариант 2: Через командную строку**
```bash
# Скачивание новых SQL файлов
./getsql.sh

# Импорт SQL файлов
docker-compose exec php-fpm sh /application/tools/app_import_sql.sh
```

### Как часто нужно обновлять базу данных?

Рекомендуемый интервал обновления:
- **SQL файлы**: ежемесячно
- **Обложки**: раз в месяц
- **Ежедневные обновления**: ежедневно (автоматически через cron)

### Сколько места занимает база данных?

Это зависит от объема данных:
- **Минимальная установка**: ~2-3 GB
- **Средняя установка**: ~10-15 GB
- **Полная библиотека**: ~30-50+ GB

### Можно ли использовать только часть библиотеки?

Да, можно. Просто скачайте нужные SQL файлы и архивы книг. Проект будет работать с любым подмножеством данных.

## OPDS и клиенты

### Что такое OPDS?

OPDS (Open Publication Distribution System) - это стандарт для распространения и каталогизации электронных публикаций. Это позволяет использовать Flibusta Local Mirror с различными читалками электронных книг.

### Как подключить OPDS клиент?

**Calibre:**
1. Откройте Calibre
2. Перейдите в "Получить книги" → "По сети" → "Добавить каталог"
3. Введите URL: `http://localhost:27100/opds/`
4. Нажмите "Добавить"

**FBReader:**
1. Откройте FBReader
2. Перейдите в "Каталоги" → "Добавить каталог"
3. Введите URL: `http://localhost:27100/opds/`
4. Сохраните

**Moon+ Reader (Android):**
1. Откройте Moon+ Reader
2. Перейдите в "Библиотека" → "OPDS"
3. Добавьте новый каталог: `http://localhost:27100/opds/`

**KOReader:**
1. Откройте KOReader
2. Перейдите в "Поиск" → "OPDS каталоги"
3. Добавьте: `http://localhost:27100/opds/`

**Aldiko (Android):**
1. Откройте Aldiko
2. Перейдите в настройки → "OPDS каталоги"
3. Добавьте: `http://localhost:27100/opds/`

### Какая версия OPDS поддерживается?

Проект поддерживает:
- **OPDS 1.0** (для старых клиентов)
- **OPDS 1.2** (для современных клиентов)

Версия автоматически определяется по User-Agent клиента. Можно также принудительно указать версию: `?opds_version=1.0` или `?opds_version=1.2`

### Почему OPDS клиент не работает?

Проверьте следующее:
1. Контейнеры запущены: `docker-compose ps`
2. URL правильный: `http://localhost:27100/opds/`
3. Порт не занят другим приложением
4. Попробуйте добавить параметр версии: `?opds_version=1.0` или `?opds_version=1.2`

### Можно ли использовать OPDS через интернет?

Да, можно. Однако для этого потребуется:
1. Настроить обратный прокси (nginx/apache)
2. Настроить HTTPS (SSL сертификат)
3. Открыть порт в firewall
4. **Осторожно**: не рекомендуется открывать доступ в интернет без авторизации

## Производительность и оптимизация

### Как ускорить работу?

**Использование SSD:**
- Используйте SSD для хранения данных
- Это значительно ускорит импорт базы данных и поиск

**Настройка PostgreSQL:**
```bash
# Редактирование конфигурации PostgreSQL
docker-compose exec postgres nano /var/lib/postgresql/data/postgresql.conf
```

Добавьте или измените параметры:
```
shared_buffers = 256MB
effective_cache_size = 1GB
maintenance_work_mem = 128MB
checkpoint_completion_target = 0.9
wal_buffers = 16MB
default_statistics_target = 100
random_page_cost = 1.1
effective_io_concurrency = 200
work_mem = 1310kB
min_wal_size = 1GB
max_wal_size = 4GB
```

**Очистка кэша:**
```bash
# Через веб-интерфейс: Сервис → Очистить кэш
# Или вручную:
docker-compose exec php-fpm rm -rf /application/cache/authors/* /application/cache/covers/*
```

### Как уменьшить использование ресурсов?

**Ограничение ресурсов контейнеров:**
Добавьте в `docker-compose.yml`:
```yaml
services:
    postgres:
        deploy:
            resources:
                limits:
                    cpus: '2'
                    memory: 2G
                reservations:
                    cpus: '1'
                    memory: 1G
```

**Уменьшение количества соединений:**
В `phpdocker/pg/init_db.sql` добавьте:
```sql
ALTER SYSTEM SET max_connections = 100;
```

### Почему сайт работает медленно?

Возможные причины:
1. **База данных не инициализирована** - выполните инициализацию БД
2. **Мало оперативной памяти** - увеличьте RAM до 4+ GB
3. **Использование HDD вместо SSD** - используйте SSD
4. **Большое количество посетителей** - настройте балансировку нагрузки
5. **Кэш не используется** - очистите и пересоздайте кэш

### Как настроить кэширование?

**Nginx кэширование:**
Уже настроено в `phpdocker/nginx/nginx.conf`:
```nginx
location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

**OPDS кэширование:**
Класс `OPDSCache` реализован, но не включен по умолчанию. Можно включить в `application/init.php`.

## Безопасность

### Как защитить проект?

**Используйте безопасный пароль:**
```bash
openssl rand -base64 24 | tr -d "=+/" | cut -c1-32
```

**Используйте Docker secrets:**
Пароль уже хранится в `secrets/flibusta_pwd.txt` с правильными правами доступа.

**Настройте HTTPS:**
Используйте обратный прокси с SSL сертификатом (см. раздел "Настройка HTTPS" в README.md).

**Настройте firewall:**
```bash
sudo ufw allow 27100/tcp
sudo ufw enable
```

**Ограничьте доступ к БД:**
В `docker-compose.yml` измените порт БД на недоступный извне:
```yaml
ports:
    - '127.0.0.1:27101:5432'  # Только локально
```

### Нужно ли делать бэкапы?

Да, обязательно! Настройте автоматические бэкапы:

```bash
# Скрипт бэкапа
nano /usr/local/bin/flibusta_backup.sh
```

```bash
#!/bin/bash
BACKUP_DIR="/path/to/backups"
DATE=$(date +%Y%m%d_%H%M%S)

docker-compose exec -T postgres pg_dump -U flibusta flibusta > "$BACKUP_DIR/flibusta_db_$DATE.sql"
```

Добавьте в crontab:
```bash
0 1 * * * /usr/local/bin/flibusta_backup.sh
```

### Как восстановить из бэкапа?

```bash
# Остановка контейнеров
docker-compose down

# Запуск только PostgreSQL
docker-compose up -d postgres

# Восстановление БД
docker-compose exec -T postgres psql -U flibusta -d flibusta < backup_20240101.sql

# Запуск всех контейнеров
docker-compose up -d
```

## Устранение проблем

### Контейнеры не запускаются

**Проверьте логи:**
```bash
docker-compose logs postgres
docker-compose logs php-fpm
docker-compose logs webserver
```

**Частые проблемы:**
1. **Порт занят** - измените порт в `.env`
2. **Недостаточно памяти** - увеличьте RAM
3. **Неверный пароль** - проверьте `.env` и `secrets/flibusta_pwd.txt`

### База данных не инициализируется

**Проверьте SQL файлы:**
```bash
ls -la FlibustaSQL/
```

**Проверьте логи импорта:**
```bash
docker-compose logs php-fpm
```

**Ручной импорт:**
```bash
./scripts/init_database.sh
```

### Ошибки при скачивании данных

**Проверьте подключение к интернету:**
```bash
ping flibusta.is
```

**Используйте зеркало (если основной сайт недоступен):**
Отредактируйте скрипты `getsql.sh`, `getcovers.sh`, `update_daily.sh` и замените URL на зеркало.

### Сайт недоступен с других устройств

**Проверьте firewall:**
```bash
sudo ufw status
sudo ufw allow 27100/tcp
```

**Проверьте, на каком адресе слушает сервер:**
```bash
sudo netstat -tuln | grep 27100
```

Если `127.0.0.1:27100` - сервер слушает только локально. Измените в `docker-compose.yml`:
```yaml
ports:
    - '0.0.0.0:27100:80'  # Слушает на всех интерфейсах
```

## Лицензия и юридические вопросы

### Законно ли использовать этот проект?

Это сложный вопрос. Законы об авторском праве различаются в разных странах. В некоторых странах использование оригинальных материалов для личного использования разрешено, в других - нет.

Убедитесь, что вы соблюдаете все применимые законы об авторском праве в вашей юрисдикции.

### Можно ли использовать проект коммерчески?

Проект распространяется под лицензией GPL v2.0, что означает, что:
- Вы можете использовать его в коммерческих целях
- Вы должны предоставить исходный код модификаций
- Вы не можете ограничивать права пользователей

Однако использование контента Флибусты может нарушать авторские права. Консультируйтесь с юристом.

### Нужно ли указывать авторство?

Да, при распространении модифицированной версии вы должны указать авторство оригинального проекта и всех контрибьюторов.

## Разработка и контрибьюция

### Как внести свой вклад?

Приветствуются любые вклады! Пожалуйста:
1. Создайте Fork проекта
2. Создайте ветку для вашей функции: `git checkout -b feature/amazing-feature`
3. Внесите изменения
4. Создайте Pull Request

### Как сообщить о баге?

Создайте Issue на GitHub: https://github.com/Zeed80/flibusta_test/issues

Включите в Issue:
- Версию Docker: `docker -v`
- Версию Docker Compose: `docker-compose --version`
- Версию PostgreSQL: `docker-compose exec postgres psql --version`
- Логи ошибок: `docker-compose logs --tail=100`
- Шаги воспроизведения

### Как запустить проект в режиме разработки?

```bash
# Клонирование репозитория
git clone https://github.com/Zeed80/flibusta_test.git
cd flibusta_test

# Использование локального объема для данных (опционально)
# cp -r /path/to/sql FlibustaSQL
# cp -r /path/to/books Flibusta.Net

# Запуск контейнеров в режиме отладки
docker-compose up

# Просмотр логов в реальном времени
docker-compose logs -f
```

### Где находится документация по API?

В настоящее время публичного API нет. Однако OPDS можно рассматривать как API для чтения. Документация по OPDS: [application/opds/README.md](../application/opds/README.md)

## Дополнительные вопросы

### Как настроить автоматическое обновление?

Добавьте в crontab:
```bash
# Ежедневное обновление в 2:00 ночи
0 2 * * * cd /path/to/flibusta && ./update_daily.sh >> /var/log/flibusta_update.log 2>&1

# Еженедельное обновление SQL файлов в воскресенье в 3:00 ночи
0 3 * * 0 cd /path/to/flibusta && ./getsql.sh && docker-compose exec php-fpm sh /application/tools/app_import_sql.sh >> /var/log/flibusta_update.log 2>&1
```

### Как настроить логирование?

**Настройка ротации логов:**
```bash
sudo nano /etc/logrotate.d/flibusta
```

```
/var/log/flibusta/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    create 0644 www-data www-data
}
```

### Как удалить проект полностью?

**⚠️ Важно: Книги и SQL файлы имеют разное поведение при удалении!**

По соображениям безопасности и удобства:
- **Книги** (Flibusta.Net) **НИКОГДА** не удаляются автоматически
- **SQL файлы** (FlibustaSQL) удаляются только с опцией `--remove-sql`

Это сделано намеренно, так как книги часто занимают много места (десятки ГБ) и могут находиться на других дисках.

```bash
# Использование скрипта удаления (рекомендуется)
chmod +x uninstall.sh
./uninstall.sh --help  # Показать все опции

# Удаление всех компонентов кроме книг (включая SQL файлы)
./uninstall.sh --all

# Скрипт с опцией --all удалит:
# - Контейнеры
# - Docker образы
# - Docker volumes (база данных)
# - Конфигурацию (.env, secrets)
# - Кэш (cache/)
# - SQL файлы (FlibustaSQL/)

# ⚠️  Книги (Flibusta.Net) НЕ будут удалены
# Удалите их вручную, если нужно:
rm -rf Flibusta.Net
```

Или вручную:
```bash
# Остановка и удаление контейнеров и volumes
docker-compose down -v
docker-compose down --rmi all -v

# Удаление SQL файлов (вручную, если нужно)
rm -rf flibusta_test/FlibustaSQL

# Переход в родительскую директорию
cd ..

# Удаление проекта
rm -rf flibusta_test

# ⚠️  Книги (Flibusta.Net) НЕ удалены автоматически
# Удалите их вручную, если нужно:
rm -rf flibusta_test/Flibusta.Net
```

Подробная документация по удалению см. в разделе [🗑️ Полное удаление проекта](../README.md#-полное-удаление-проекта) в README.md.

### Где искать дополнительную информацию?

- [README.md](../README.md) - общая документация
- [docs/INSTALL.md](INSTALL.md) - руководство по установке
- [docs/UPDATE.md](UPDATE.md) - руководство по обновлению
- [application/opds/README.md](../application/opds/README.md) - документация OPDS
- GitHub Issues: https://github.com/Zeed80/flibusta_test/issues

Если вы не нашли ответ на свой вопрос, создайте Issue на GitHub.

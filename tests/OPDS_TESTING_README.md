# Тестирование OPDS режима Flibusta

## 📋 Описание

Этот файл содержит инструкции по запуску и интерпретации результатов тестирования OPDS (Open Publication Distribution System) режима библиотеки Flibusta.

## 🚀 Как запустить тесты

### Вариант 1: Через Docker (рекомендуется)

```bash
# Запуск тестов в контейнере PHP-FPM
docker exec -it flibusta_php-fpm_1 php /application/tests/test_opds.php

# Если имя контейнера другое (посмотрите через docker ps)
docker exec -it flibusta_php-fpm php /application/tests/test_opds.php
```

### Вариант 2: Через CLI (если PHP установлен локально)

```bash
# Запуск тестов напрямую PHP
php /path/to/flibusta/application/tests/test_opds.php
```

### Вариант 3: Через браузер (для визуальной проверки)

```bash
# Тесты будут доступны по URL
http://localhost:27100/tests/test_opds.php
```

## 📊 Что проверяют тесты

| Тест | Описание | Проверяет |
|------|-----------|----------|
| Главная страница | `/opds/` | Валидность XML, OPDS namespace, обязательные элементы |
| Список новинок | `/opds/list/` | Правильные rel типы для acquisition, пагинация |
| Поиск книг | `/opds/search?q=` | Поисковый функционал, результаты |
| Поиск авторов | `/opds/search?by=author&q=` | Поиск авторов по фамилии |
| Жанры | `/opds/genres` | Список категорий жанров |
| Поджанры | `/opds/listgenres` | Поджанры конкретного жанра |
| Индекс авторов | `/opds/authorsindex` | Алфавитный индекс авторов |
| Индекс серий | `/opds/sequencesindex` | Алфавитный индекс книжных серий |
| Книжные полки | `/opds/favs` | Список пользовательских книжных полок |
| Избранное пользователя | `/opds/fav?uuid=` | Книги из избранного |
| Пагинация | `/opds/list/?page=2` | Навигационные ссылки (first, prev, next, last) |
| OPDS 1.0 версия | `?opds_version=1.0` | Обратная совместимость |
| OPDS 1.2 версия | `?opds_version=1.2` | Новая версия с фасетами |
| Пустой поиск | `/opds/search?q=` | Обработка пустого запроса |
| ETag кэширование | Повторные запросы | Кэш с 304 Not Modified |

## 🎯 Критерии успешного прохождения

### Обязательные проверки:
- ✅ XML валидный и правильно сформирован
- ✅ Присутствует OPDS namespace (http://opds-spec.org/2010/catalog или https://specs.opds.io/opds-1.2)
- ✅ Все обязательные элементы: `<feed>`, `<id>`, `<title>`, `<updated>`
- ✅ Правильный Content-Type: `application/atom+xml; charset=utf-8`

### Для acquisition фидов (книги):
- ✅ Правильный rel тип: `http://opds-spec.org/acquisition`
- ✅ Правильные MIME-типы для разных форматов
- ✅ Присутствуют ссылки на скачивание
- ✅ Присутствуют ссылки на обложки (image/jpeg)

### Для навигационных фидов (индексы):
- ✅ Правильный rel тип: `http://opds-spec.org/subsection` или `http://opds-spec.org/acquisition`
- ✅ Наличие записей для навигации
- ✅ Пагинация работает корректно (если применимо)

### Для OPDS 1.2:
- ✅ Правильный namespace: `https://specs.opds.io/opds-1.2`
- ✅ Профиль: `application/atom+xml;profile=opds-catalog;kind=acquisition` или `kind=navigation`
- ✅ Фасетная навигация (opds:facetGroup)
- ✅ Метаданные пагинации: `opds:numberOfItems`, `opds:itemsPerPage`

## 📋 Интерпретация результатов

### Успешный тест (✅ PASSED):
```
ТЕСТ: Главная страница OPDS (/)
Результат: ✅ PASSED
Сообщение: Все проверки пройдены
```

**Что значит:** OPDS фид корректно сформирован и соответствует спецификации.

---

### Неудачный тест (❌ FAILED):
```
ТЕСТ: Список новинок (/opds/list/)
Результат: ❌ FAILED
Сообщение: HTTP код: 200
Ошибка: Отсутствует правильный rel тип acquisition для OPDS 1.2
```

**Что значит:** Есть проблема, которую нужно исправить. Обратите внимание на сообщение об ошибке.

---

### Частые проблемы и решения:

#### Проблема 1: "В каталоге не указана необходимая информация"
**Причины:**
- ❌ Отсутствует элемент `<feed>` или `<id>`
- ❌ Отсутствует элемент `<title>`
- ❌ Отсутствует OPDS namespace
- ❌ Неверный Content-Type

**Решения:**
- Проверьте, что `$feed->setId()` и `$feed->setTitle()` вызываются
- Проверьте, что `$feed->render()` вызывается и выводится
- Проверьте, что используются правильные классы OPDS

#### Проблема 2: "Ошибка 500"
**Причины:**
- ❌ SQL ошибки в запросах к БД
- ❌ PHP fatal errors
- ❌ Отсутствие необходимых файлов или функций

**Решения:**
- Проверьте логи PHP-FPM: `docker logs flibusta_php-fpm_1 --tail=50`
- Проверьте подключение к БД
- Проверьте, что все OPDS файлы существуют и корректны

#### Проблема 3: "Не найдено" (404)
**Причины:**
- ❌ Неверный URL в ссылках
- ❌ Отсутствие записи в БД

**Решения:**
- Проверьте `$webroot` в конфигурации
- Проверьте, что путь в OPDSLink правильный
- Проверьте, что записи существуют в БД

#### Проблема 4: "Крякозябры"
**Причины:**
- ❌ Неверная кодировка текста (не UTF-8)
- ❌ Проблемы с нормализацией текста
- ❌ Отсутствие экранирования специальных символов

**Решения:**
- Проверьте, что используется `ENT_XML1, 'UTF-8'` для экранирования XML
- Проверьте, что заголовок `charset=utf-8` установлен
- Проверьте функцию `normalize_text_for_opds()` если используется

#### Проблема 5: "Отображаются значки без названий"
**Причины:**
- ❌ Тек заголовков не передаётся
- ❌ Проблемы с SQL запросами (неверные имена полей)
- ❌ Записи в БД пустые

**Решения:**
- Проверьте SQL запросы на правильность имён полей (BookId, Title, etc.)
- Проверьте, что данные действительно присутствуют в БД
- Проверьте, что `$b->title` не пустой

## 🔍 Диагностика

### Если тесты не запускаются:

1. **Проверьте имя контейнера:**
   ```bash
   docker ps
   ```
   Ищите контейнер с именем, содержащим "php-fpm"

2. **Проверьте запущен ли контейнер:**
   ```bash
   docker ps | grep php-fpm
   ```
   Статус должен быть "Up"

3. **Проверьте файл теста:**
   ```bash
   ls -la /path/to/flibusta/application/tests/test_opds.php
   ```
   Файл должен существовать

4. **Проверьте права на файл теста:**
   ```bash
   docker exec flibusta_php-fpm_1 ls -la /application/tests/test_opds.php
   ```
   Права должны позволять чтение и выполнение

### Если тесты выполняются, но OPDS не работает:

1. **Перезапустите PHP-FPM:**
   ```bash
   docker-compose restart php-fpm
   ```

2. **Очистите кэш OPDS:**
   ```bash
   docker exec flibusta_php-fpm_1 rm -rf /application/cache/opds/*
   ```

3. **Проверьте логи на ошибки:**
   ```bash
   docker logs flibusta_php-fpm_1 --tail=100 | grep -i error
   ```

4. **Проверьте подключение к базе данных:**
   - Откройте http://localhost:27100/service/
   - Посмотрите, нет ли ошибок подключения

## 📊 Статистика тестирования

После запуска тесты выведут:

```
╔═══════════════════════════════════╗
║     OPDS ТЕСТОВАНИЕ СИСТЕМЫ          ║
╚═════════════════════════════════════╝

Базовый URL: http://localhost:27100/opds/
Запуск: 2024-12-26 15:30:45

========================================
ТЕСТ: Главная страница OPDS (/)
Результат: ✅ PASSED
...

[дальнейшие тесты]

========================================
СВОДНАЯ СТАТИСТИКА ТЕСТОВ
Всего тестов: 25
Пройдено: 23 (92%)
Провалено: 2 (8%)
Успешность: 92%

========================================
```

### Процент успешности:
- **90-100%**: 🎉 Отлично! Всё работает идеально
- **80-90%**: ✨ Хорошо! Небольшие проблемы, но OPDS функционален
- **50-80%**: ⚠️ Средне. Есть проблемы, которые нужно исправить
- **менее 50%**: ❌ Критически. Требуется срочное исправление

## 📚 Связанная документация

- [OPDS 1.0 Specification](http://opds-spec.org/2010/catalog)
- [OPDS 1.2 Specification](https://specs.opds.io/opds-1.2)
- [Atom Syndication Format](https://tools.ietf.org/html/rfc4287)

## 💡 Советы по исправлению проблем

1. **Сначала исправьте критические ошибки**, которые ломают весь OPDS режим
2. **Затем протестируйте каждую исправленную версию**
3. **Используйте кэширование для ускорения тестов**
4. **Не забудьте очистить кэш после исправлений**

## 🔧 После исправлений

1. Перезапустите PHP-FPM: `docker-compose restart php-fpm`
2. Запустите тесты заново: `php /application/tests/test_opds.php`
3. Сравните результаты до и после
4. Убедитесь, что все тесты проходят успешно

## 📞 Поддержка

Если после выполнения всех шагов OPDS всё ещё не работает:

1. **Проверьте конфигурацию веб-сервера** (nginx)
2. **Убедитесь, что контейнеры работают**: `docker-compose ps`
3. **Проверьте логи**: `docker-compose logs php-fpm --tail=200`
4. **Создайте issue в репозитории** с подробным описанием проблемы

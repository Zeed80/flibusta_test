# Flibusta Local Mirror - Конфигурация
# Скопируйте этот файл в .env и настройте параметры под ваши нужды

# Пользователь базы данных
FLIBUSTA_DBUSER=flibusta

# Имя базы данных
FLIBUSTA_DBNAME=flibusta

# Тип базы данных
FLIBUSTA_DBTYPE=postgres

# Хост базы данных
FLIBUSTA_DBHOST=postgres

# Пароль базы данных (обязательно смените на безопасный!)
FLIBUSTA_DBPASSWORD=your_secure_password_here

# Базовый путь веб-приложения (обычно пусто)
FLIBUSTA_WEBROOT=

# Порт веб-сервера (измените, если порт 27100 занят)
FLIBUSTA_PORT=27100

# Порт базы данных (измените, если порт 27101 занят)
FLIBUSTA_DB_PORT=27101

# Версия PHP (по умолчанию 8.2)
FLIBUSTA_PHP_VERSION=8.2

# Путь к книгам (если они находятся в другой директории)
# По умолчанию: ./Flibusta.Net (относительный путь)
# Для внешних дисков: /path/to/external/books (абсолютный путь)
# Пример: FLIBUSTA_BOOKS_PATH=/media/external_drive/books
FLIBUSTA_BOOKS_PATH=./Flibusta.Net

# Путь к SQL файлам (если они находятся в другой директории)
# По умолчанию: ./FlibustaSQL (относительный путь)
# Для внешних дисков: /path/to/external/sql (абсолютный путь)
# Пример: FLIBUSTA_SQL_PATH=/media/external_drive/sql
FLIBUSTA_SQL_PATH=./FlibustaSQL

# Порт Prometheus (по умолчанию 9090)
# Измените, если порт 9090 занят
FLIBUSTA_PROMETHEUS_PORT=9090
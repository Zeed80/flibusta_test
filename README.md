# Flibusta Local Mirror

Docker-контейнер для локальной копии библиотеки Флибусты с веб-интерфейсом и современной поддержкой OPDS 1.2.

> **Примечание**: Этот проект является форком оригинального репозитория [zlsl/flibusta](https://github.com/zlsl/flibusta) с существенными улучшениями и модернизацией.

**Репозиторий проекта**: [https://github.com/Zeed80/flibusta_test](https://github.com/Zeed80/flibusta_test)

## 🚀 Возможности

### Основные функции
- 📚 Отображение книг с поиском по заголовкам, сборникам, авторам, жанрам
- 📖 Поддержка форматов: fb2, docx, rtf, mobi, epub, txt, html, pdf, djvu
- 💾 Сохранение позиции чтения для fb2
- 📑 Создание "книжных полок" для избранных книг, авторов и сборников
- 🐳 Полная контейнеризация с Docker Compose

### Новые возможности OPDS (v2.0)
- ✨ **Полная поддержка OPDS 1.2** с автоматическим определением версии клиента
- 🔄 **Обратная совместимость** с OPDS 1.0 для старых клиентов
- 📄 **Пагинация** с навигационными ссылками (first, previous, next, last)
- 🔍 **Фасетная навигация** с фильтрами по языку и формату файла
- 🔎 **Улучшенный поиск** с полнотекстовым поиском по названию, автору и аннотации
- 📊 **Расширенные метаданные** с правильными MIME-типами и Dublin Core элементами
- 🛡️ **Улучшенная безопасность** с защитой от SQL-инъекций и XSS
- ⚡ **Готовность к кэшированию** для повышения производительности

## 📸 Скриншоты

|![x](blob/x1.png)|![x](blob/x2.png)|![x](blob/x3.png)|![x](blob/x4.png)|
|---|---|---|---|
## 📋 Требования

- Docker и Docker Compose
- Файлы дампа базы данных Флибусты (*.sql)
- Архивы книг Флибусты (*.zip)

## 🔧 Установка

### Требования

- Docker и Docker Compose (версия 3.1 или выше)
- Минимум 2 GB свободного места на диске
- Файлы дампа базы данных Флибусты (*.sql или *.sql.gz)
- Архивы книг Флибусты (*.zip)

### Пошаговая установка

#### 1. Клонирование репозитория

```bash
git clone https://github.com/Zeed80/flibusta_test.git
cd flibusta_test
```

Или скачайте архив и распакуйте его:
```bash
wget https://github.com/Zeed80/flibusta_test/archive/main.zip
unzip main.zip
cd flibusta_test-main
```

#### 2. Создание необходимых директорий

```bash
mkdir -p FlibustaSQL Flibusta.Net cache secrets
mkdir -p cache/authors cache/covers cache/tmp cache/opds
```

#### 3. Настройка пароля базы данных

Создайте файл с паролем для базы данных:

**Linux/Mac:**
```bash
echo "your_secure_password" > secrets/flibusta_pwd.txt
chmod 600 secrets/flibusta_pwd.txt
```

**Windows (PowerShell):**
```powershell
"your_secure_password" | Out-File -FilePath secrets\flibusta_pwd.txt -Encoding utf8 -NoNewline
icacls secrets\flibusta_pwd.txt /grant:r "Users:(R)"
```

> ⚠️ **Важно**: Замените `your_secure_password` на надежный пароль!

#### 4. Размещение данных Флибусты

**Файлы дампа базы данных:**
- Разместите файлы `*.sql` или `*.sql.gz` в каталог `FlibustaSQL/`
- Поддерживаются файлы: `lib.*.sql`, `lib.*.sql.gz`

**Архивы книг:**
- Разместите архивы `*.zip` в каталог `Flibusta.Net/`
- Файлы должны быть в формате, используемом Флибустой

**Опционально - архивы изображений:**
- `lib.a.attached.zip` - фотографии авторов (в `cache/`)
- `lib.b.attached.zip` - обложки книг (в `cache/`)

#### 5. Настройка прав доступа

**Linux/Mac:**
```bash
# Права на запись для кэша
chmod -R 777 cache

# Права на выполнение для скриптов
chmod +x application/tools/*.sh
chmod +x application/tools/app_*

# Права для FlibustaSQL (если нужно)
chmod -R 755 FlibustaSQL
```

**Windows:**
Права обычно настраиваются автоматически, но убедитесь, что:
- Папка `cache` доступна для записи
- Папка `FlibustaSQL` доступна для чтения

#### 6. Настройка Docker Compose (опционально)

При необходимости измените параметры в `docker-compose.yml`:

```yaml
services:
  postgres:
    ports:
      - '27101:5432'  # Измените порт при необходимости
  webserver:
    ports:
      - '27100:80'    # Измените порт веб-сервера
```

#### 7. Сборка и запуск контейнеров

```bash
# Сборка образов
docker-compose build

# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов (опционально)
docker-compose logs -f
```

#### 8. Инициализация базы данных

1. Откройте в браузере: **http://localhost:27100**
2. Перейдите в меню **"Сервис"**
3. Нажмите **"Обновить базу"**
4. Дождитесь завершения процесса (может занять до 30 минут)

> 💡 **Совет**: Процесс импорта можно отслеживать через веб-интерфейс или логи Docker

#### 9. Проверка работы

- **Веб-интерфейс**: http://localhost:27100
- **OPDS каталог**: http://localhost:27100/opds/
- **OpenSearch**: http://localhost:27100/opds-opensearch.xml.php

### ⚠️ Важные замечания

- Каталоги `FlibustaSQL`, `cache` и их подкаталоги должны иметь права на запись
- Скрипты в каталоге `/application/tools/` должны иметь права на выполнение
- Для обновления базы с новыми файлами повторите пункты 4, 5, 6
- Рекомендуется использовать SSD для ускорения импорта базы данных

## ⚙️ Конфигурация

### Переменные окружения

Параметры базы данных можно определить в `docker-compose.yml`. 

Доступные переменные окружения:

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `FLIBUSTA_DBUSER` | Пользователь базы данных | `flibusta` |
| `FLIBUSTA_DBNAME` | Имя базы данных | `flibusta` |
| `FLIBUSTA_DBTYPE` | Тип базы данных | `postgres` |
| `FLIBUSTA_DBHOST` | Хост базы данных | `postgres` |
| `FLIBUSTA_DBPASSWORD` | Пароль базы данных | `flibusta` |
| `FLIBUSTA_WEBROOT` | Базовый путь веб-приложения | `` (пусто) |

### Настройка OPDS

Все настройки OPDS находятся в файлах `application/opds/`. Основные параметры:

- **Количество записей на странице**: определяется константой `OPDS_FEED_COUNT` в `application/init.php` (по умолчанию: 100)
- **Версия OPDS**: автоматически определяется по User-Agent клиента
- **Принудительная версия**: добавьте параметр `?opds_version=1.0` или `?opds_version=1.2` к URL

### Безопасность паролей

⚠️ **Важно**: Docker Compose не рекомендует хранить пароли в переменных окружения или в коде.

Для безопасного хранения паролей используйте механизм `secrets`:

1. Создайте текстовый файл с паролем:
```bash
echo "your_secure_password" > secrets/flibusta_pwd.txt
```

2. Установите правильные права доступа:
```bash
# Для Linux/Mac
sudo chown 82:82 secrets/flibusta_pwd.txt
chmod 600 secrets/flibusta_pwd.txt

# Для Windows (в PowerShell от администратора)
icacls secrets\flibusta_pwd.txt /grant:r "82:(R)"
```

Пользователь с ID 82 - это стандартный пользователь `www-data` из официального образа PHP-FPM.  
   
## 🆕 Новые возможности OPDS 2.0

### Полная поддержка OPDS 1.2

Проект полностью обновлен до последней версии спецификации OPDS 1.2 с сохранением обратной совместимости:

- ✅ Правильные namespace и profile атрибуты
- ✅ Расширенные метаданные (dc:language, dc:subject, opds:numberOfItems)
- ✅ Правильные MIME-типы для всех форматов файлов
- ✅ Улучшенная структура ссылок

### Автоматическое определение версии клиента

Система автоматически определяет версию OPDS клиента по User-Agent и генерирует соответствующий фид:

- **OPDS 1.2** для современных клиентов (Calibre, KOReader, Aldiko)
- **OPDS 1.0** для старых клиентов (FBReader, Moon+ Reader)
- **Автоматический fallback** на OPDS 1.2 если версия не определена

### Пагинация

Все списки книг теперь поддерживают полноценную пагинацию:

- Навигационные ссылки: first, previous, next, last
- Метаданные пагинации для OPDS 1.2 (numberOfItems, itemsPerPage)
- Параметр `?page=N` для перехода на нужную страницу

**Пример:**
```
http://localhost:27100/opds/list/?page=2
http://localhost:27100/opds/list/?author_id=123&page=3
```

### Фасетная навигация

Фильтрация книг по различным критериям (только для OPDS 1.2):

- **По языку**: фильтрация по языку книги
- **По формату**: фильтрация по типу файла (fb2, epub, pdf и т.д.)
- Активные фильтры отображаются в фиде

**Пример:**
```
http://localhost:27100/opds/list/?lang=ru
http://localhost:27100/opds/list/?format=epub
```

### Улучшенный поиск

Полнотекстовый поиск по нескольким полям:

- Поиск по названию книги
- Поиск по имени автора
- Поиск по аннотации
- Использование PostgreSQL ILIKE для нечувствительного к регистру поиска

**Пример:**
```
http://localhost:27100/opds/search?q=толстой
http://localhost:27100/opds/search?by=author&q=пушкин
```

### Расширенные метаданные

Улучшенные метаданные для каждой книги:

- **Dublin Core элементы**: dc:language, dc:subject, dc:format, dc:issued
- **Правильные MIME-типы**: 
  - `application/fb2+zip` для FB2
  - `application/epub+zip` для EPUB
  - `application/x-mobipocket-ebook` для MOBI
  - И другие стандартные типы
- **Размер файла**: dcterms:extent с человекочитаемым форматом

### Улучшенная безопасность

Все уязвимости безопасности исправлены:

- ✅ Все SQL-запросы используют prepared statements
- ✅ Валидация всех входных параметров
- ✅ Экранирование всех выходных данных
- ✅ Защита от SQL-инъекций и XSS атак

### Готовность к кэшированию

Реализован класс `OPDSCache` для повышения производительности:

- Кэширование статических фидов
- Поддержка ETag и Last-Modified заголовков
- Инвалидация кэша при обновлении данных

### Структура OPDS API

```
/opds/                    - Главная страница каталога
/opds/list/               - Список книг (с пагинацией)
/opds/list/?genre_id=N    - Книги по жанру
/opds/list/?author_id=N   - Книги автора
/opds/list/?seq_id=N      - Книги серии
/opds/search?q=query      - Поиск книг
/opds/search?by=author&q=query - Поиск авторов
/opds/genres              - Список категорий жанров
/opds/authorsindex        - Индекс авторов
/opds/sequencesindex      - Индекс серий
/opds/favs                - Список пользовательских полок
/opds/fav?uuid=...        - Избранное пользователя
/opds/author?author_id=N  - Страница автора
```

## 🔄 Использование с внешними сервисами

По умолчанию Docker Compose поднимает веб-сервер и базу данных для локального зеркала Флибусты. 

Если у вас уже есть веб-сервер (например, на NAS) и база данных, вы можете использовать их вместо контейнеров, что позволит сэкономить ресурсы сервера.

Папка `application/tools/external_services_config` содержит необходимые скрипты и конфигурационные файлы для работы в такой конфигурации. Подробнее см. [README](application/tools/external_services_config/README.md).

## 📖 Использование

### Веб-интерфейс
- **Основной портал**: http://localhost:27100
- **OPDS каталог**: http://localhost:27100/opds/
- **OpenSearch описание**: http://localhost:27100/opds-opensearch.xml.php

### Подключение OPDS клиентов

#### Calibre
1. Откройте Calibre
2. Перейдите в "Получить книги" → "По сети" → "Добавить каталог"
3. Введите URL: `http://localhost:27100/opds/`
4. Нажмите "Добавить"

#### FBReader
1. Откройте FBReader
2. Перейдите в "Каталоги" → "Добавить каталог"
3. Введите URL: `http://localhost:27100/opds/`
4. Сохраните

#### Moon+ Reader
1. Откройте Moon+ Reader
2. Перейдите в "Библиотека" → "OPDS"
3. Добавьте новый каталог: `http://localhost:27100/opds/`

#### KOReader
1. Откройте KOReader
2. Перейдите в "Поиск" → "OPDS каталоги"
3. Добавьте: `http://localhost:27100/opds/`

### Обновление базы данных
1. Разместите новые файлы дампа (*.sql или *.sql.gz) в `FlibustaSQL`
2. Разместите новые архивы (*.zip) в `Flibusta.Net`
3. Перезапустите контейнеры: `docker-compose restart`
4. Выполните "Обновление базы" через веб-интерфейс (меню "Сервис")

### Очистка кэша
Через веб-интерфейс: меню "Сервис" → "Очистить кэш"

Или вручную:
```bash
docker-compose exec php-fpm rm -rf /application/cache/authors/* /application/cache/covers/*
```

## 🚀 Публикация на GitHub

Для публикации проекта на GitHub используйте готовые скрипты:

**Windows (PowerShell):**
```powershell
.\publish_to_github.ps1 -RemoteUrl "https://github.com/Zeed80/flibusta_test.git"
```

**Linux / macOS:**
```bash
chmod +x publish_to_github.sh
./publish_to_github.sh https://github.com/Zeed80/flibusta_test.git
```

Подробные инструкции см. в [PUBLISHING.md](PUBLISHING.md).

## 🐛 Устранение неполадок

### Проблемы с подключением к базе данных

```bash
# Проверка статуса контейнеров
docker-compose ps

# Просмотр логов PostgreSQL
docker-compose logs postgres

# Перезапуск контейнеров
docker-compose restart
```

### Проблемы с импортом базы данных

- Убедитесь, что файлы `*.sql` или `*.sql.gz` находятся в `FlibustaSQL/`
- Проверьте права доступа к папке `FlibustaSQL`
- Проверьте логи через веб-интерфейс (меню "Сервис")

### Проблемы с OPDS клиентами

- Проверьте доступность URL: `http://localhost:27100/opds/`
- Убедитесь, что порт 27100 не занят другим приложением
- Попробуйте добавить параметр версии: `?opds_version=1.0` или `?opds_version=1.2`

### Проблемы с производительностью

- Используйте SSD для ускорения работы базы данных
- Увеличьте лимиты памяти для контейнеров в `docker-compose.yml`
- Очистите кэш через веб-интерфейс

## 📚 Дополнительная документация

- [Документация OPDS](application/opds/README.md) - подробное описание OPDS функций
- [Конфигурация внешних сервисов](application/tools/external_services_config/README.md) - использование с внешними сервисами
- [Инструкция по публикации](PUBLISHING.md) - публикация проекта на GitHub

## 🔄 Обновление проекта

Для обновления до последней версии:

```bash
# Остановка контейнеров
docker-compose down

# Получение обновлений из репозитория
git pull https://github.com/Zeed80/flibusta_test.git main

# Или если remote уже настроен:
git pull origin main

# Пересборка и запуск
docker-compose build
docker-compose up -d
```

> ⚠️ **Важно**: Перед обновлением сделайте резервную копию базы данных и файлов конфигурации!

## 📝 Лицензия

Этот проект распространяется под лицензией [GNU General Public License v2.0](LICENSE).

## 🔗 Оригинальный проект

Этот репозиторий является форком оригинального проекта:
- **Оригинальный репозиторий**: [zlsl/flibusta](https://github.com/zlsl/flibusta)
- **Автор оригинального проекта**: [@zlsl](https://github.com/zlsl)

## 🤝 Вклад в проект

Приветствуются любые вклады! Пожалуйста, создавайте Issues и Pull Requests.

### Планы развития

- [ ] Интеграция кэширования в основные фиды
- [ ] Поддержка OPDS-PSE для потоковой передачи
- [ ] Расширенные метаданные (ISBN, издательство)
- [ ] Поддержка аудиокниг
- [ ] API для разработчиков

## ⚠️ Отказ от ответственности

Этот проект предназначен только для личного использования и создания локальной копии библиотеки Флибусты. Убедитесь, что вы соблюдаете все применимые законы об авторском праве в вашей юрисдикции.


